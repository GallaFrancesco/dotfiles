# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

workspace 1 output eDP1
workspace 2 output HDMI1
workspace 3 output eDP1
workspace 4 output HDMI1
workspace 5 output eDP1
workspace 6 output HDMI1
workspace 7 output eDP1
workspace 8 output HDMI1
workspace 9 output eDP1

#assign [class="Firefox"] 2
mouse_warping output

set $bg #191919
set $fg #817f84

set $lime      #9FBC00
set $darklime  #505e00
set $blue      #131D24
set $lightblue #232D34
set $lightgray #DDEEDD
set $darkgray  #444444
set $white     #FFFFFF
set $urgentred #B33A3A
set $focus #A2A2A2
set $unfocus #838383
set $borderf #c3c3c3
set $borderu #eeeeee

set $mod Mod4

# Layout colors 	      bord 	   bg 	    text     indicator (split)
client.focused 		      $focus   $borderf $blue   $borderf
client.focused_inactive   $unfocus $unfocus $blue $unfocus
client.unfocused 	      $unfocus $borderu $blue $borderu
client.urgent 		      $focus   $focus   $blue   $focus
client.background 	      $unfocus

# no borders for new windows (not floating) 
new_window none
# border for floating ones
new_float normal
## hide edge borders for both window types
#hide_edge_borders both

# toggle window border
bindsym $mod+Shift+space mark --add _; [con_mark=f] floating disable, border none, mark --add --toggle _; [con_mark=_] floating enable, border pixel 5; mark --add --toggle f;

#gaps
#gaps inner 5
#gaps outer 5

#startup [NW] means not working 
exec --no-startup-id redshift -t 6500:3000									# start redshift
exec --no-startup-id /bin/bash .screenlayout/myres.sh						# set resolution (one/two monitors)
exec --no-startup-id setxkbmap us											# set the keyboard to US layout
exec --no-startup-id sudo python2 /etc/hda-headphones.py					# [NW] set audio device
exec --no-startup-id sudo bumblebeed										# start the optirun bumblebee service (here so that it's surely started after X)
exec --no-startup-id xbindkeys												# bind the modified keys (mouse only now)
exec --no-startup-id synclient tapButton1=1									# set synaptics touchpad tap
exec --no-startup-id synclient HorizTwoFingerScroll=1						# set synaptics touchpad scroll
exec --no-startup-id compton												# start the composite manager
exec --no-startup-id urxvtd  --quiet --opendisplay --fork					# start the urxvt daemon
exec --no-startup-id bash /home/francesco/.set_bg.sh						# set the background (randomly from /home/Images folder)
exec --no-startup-id xrdb -load /home/francesco/.Xdefaults					# load X default settings

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# [NW] here for compatibility, window titles are NOT displayed
font pango: Inconsolata 8

#action keys: volume
bindsym XF86AudioRaiseVolume exec amixer -q -c 2 set Master 2dB+ unmute
bindsym XF86AudioLowerVolume exec amixer -q -c 2 set Master 2dB- unmute
bindsym XF86AudioMute exec amixer -q -c 2 set Master toggle

#action keys: brightness
# Screen brightness controls
bindsym XF86MonBrightnessUp exec xbacklight -inc 15 &  # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 15 & # decrease screen brightness

#toggle touchpad (through script)
bindsym $mod+Shift+t exec /home/francesco/toggletouch.sh

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec urxvtc

# telegram
bindsym $mod+m exec /home/francesco/Telegram/Telegram
##ncmpcpp
#bindsym $mod+n exec sakura -e ncmpcpp

#transmission
bindsym $mod+t exec transmission-remote-gtk

#vim
bindsym $mod+Shift+v exec sakura -e vim 

#htop
bindsym $mod+Shift+h exec sakura -e htop

# kill focused window
bindsym $mod+q kill

#jump to first empty
#bindsym Scroll_Lock exec slock

#browser
bindsym $mod+b exec optirun firefox-bin

#thunderbird
bindsym $mod+Shift+b exec sylpheed

# start dmenu (a program launcher)
bindsym $mod+space exec dmenu_run -sb "#9c6c9d" -sf "#f2f2f2" -nb "#3a3a3a" -dim 0.6 -fn "Inconsolata:size=11" -x 0 -y 0 -l 60 -w 200
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
#bindsym $mod+Shift+space floating toggle

 #sticky and resize
bindsym $mod+Shift+s floating enable, sticky toggle, resize set 180 180;
# toggle sticky
#bindsym $mod+Shift+s sticky toggle

# change focus between tiling / floating windows
#bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# fibonacci layout on spawning windows
# ------------
# |      | B |
# |  A   |---|
# |      | C |
# ------------
default_orientation vertical
for_window [class=".*"] split toggle #toggles split at each new window
bindsym $mod+shift+q split toggle kill #kill command resets the split
#bindsym $mod+e layout toggle split # should already exist but use if split orientation  messes up

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
		i3bar_command     i3bar
		#status_command    python3 /home/francesco/i3pystatus_config.py
		status_command    i3blocks
        	position	  bottom
		workspace_buttons yes
		separator_symbol  "  "
		font pango: 	  Inconsolata 8

		colors {
			background 		#000000
			focused_workspace 	#2885CC #00331aD0 #ffffff
			active_workspace 	#004d26 #000000D0 #FFFFFF
			inactive_workspace 	#000000D0 #000000D0 #FFFFFFD0
		}
}


